<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fullstack.shop.report.dao.ReportDao">
    
	<select id="delByCountDate" parameterType="java.lang.String">
		delete 
		
		from 
			shop_report 
		where 
			count_date=#{countDate}
    </select>  
    
    <select id="groupByYearMonth" resultType="java.util.HashMap">
		select 
			DATE_FORMAT(count_date,'%Y') as `year`, 
			DATE_FORMAT(count_date,'%m') as `month`
		from 
			shop_report 
		group by DATE_FORMAT(count_date,'%Y-%m') 
		<!-- order by count_date asc -->
    </select> 
	
	<select id="getDataGroupByYear" resultType="java.util.HashMap">
		select 
			DATE_FORMAT(count_date,'%Y') as `key`, 
			sum(total) as `val`
		from 
			shop_report 
		group by DATE_FORMAT(count_date,'%Y') 
    </select> 
    
    <select id="getDataGroupByMonth" resultType="java.util.HashMap">
		select 
			DATE_FORMAT(count_date,'%m') as `key`, 
			sum(total) as `val`
		from 
			shop_report 
		<where>
			<if test="countDate!=null and countDate!=''">
				and INSTR(count_date,#{countDate}) &gt; 0
			</if>
		</where>
		group by DATE_FORMAT(count_date,'%Y-%m') 
    </select> 
    
    <select id="getDataGroupByDay" resultType="java.util.HashMap">
		select 
			DATE_FORMAT(count_date,'%d') as `key`, 
			sum(total) as `val`
		from 
			shop_report 
		<where>
			<if test="countDate!=null and countDate!=''">
				and INSTR(count_date,#{countDate}) &gt; 0
			</if>
		</where>
		group by DATE_FORMAT(count_date,'%Y-%m-%d') 
    </select> 
	
</mapper>